snippet rco "simple react component"
        import React from 'react'

        type Props = {
        }

        const ${1} = (props: Props) => {
          const {} = props
          return (
          )
        }

        export default React.memo(${1})

snippet sco "simple colid component"
        type Props = {}

        function ${1}(props: Props) {
          return ()
        }

        export default ${1}
