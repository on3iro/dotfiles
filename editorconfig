# top-most EditorConfig file
root = true

# Unix-style newlines with a newline ending every file
[*]
indent_style = space
charset = utf-8
indent_size = 4
end_of_line = lf

[*.{js,jsx,css,sass,scss,html,md}]
trim_trailing_whitespace = true
